# * group into section by rank value
# * add introspect
# * swipe to record/clear

* try to use IB to connect table and cell
* weibo
* detail for behavior
* icloud

* introduce config file
* bundle zh name 
* split into two view by rank +/-
* history summary
* history detail
* core data persistent
* input known behavior rule data
* different version of behavior rule
* paper folding animation
* pinch to close section
* when one section longer than page, close animation not so smooth